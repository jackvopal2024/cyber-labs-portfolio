# System Log Analyzer

A simple Python script that analyzes authentication logs and counts failed login attempts by source IP address.

## What it does
- Reads an authentication log file
- Identifies failed SSH login attempts
- Aggregates failures by IP address
- Outputs results to the terminal and a CSV file

## Usage
```powershell
python log_analyzer.py
